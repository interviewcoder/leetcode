/**
 ***************************************************************************
 * Description:
 * 
 * Implement an iterator over a binary search tree (BST). Your iterator will 
 * be initialized with the root node of a BST. 
 * 
 * Calling next() will return the next smallest number in the BST. 
 * 
 * Note: next() and hasNext() should run in average O(1) time and uses O(h) 
 * memory, where h is the height of the tree.
 * 
 ***************************************************************************
 * @tag : Tree; Stack
 * {@link https://leetcode.com/problems/binary-search-tree-iterator/ }
 */
package _173_BinarySearchTreeIterator;

import com.leetcode.TreeNode;

/** see test {@link _173_BinarySearchTreeIterator.PracticeTest } */
public class Practice {

    public Practice(TreeNode root) {
        //TODO
    }

    /** @return whether we have a next smallest number */
    public boolean hasNext() {
        //TODO
        return false;
    }

    /** @return the next smallest number */
    public int next() {
        //TODO
        return 0;
    }

}

/**
 * Your BSTIterator will be called like this:
 * Practice i = new Practice(root);
 * while (i.hasNext()) v[f()] = i.next();
 */
